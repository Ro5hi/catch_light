<!DOCTYPE html>
<html style="padding: 95px;">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title>PhotoBook</title>
    <link rel="stylesheet" href="assets/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Lato:300,400,700">
    <link rel="stylesheet" href="assets/fonts/font-awesome.min.css">
    <link rel="stylesheet" href="assets/css/Custom-File-Upload.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/pikaday/1.6.1/css/pikaday.min.css">
    <link rel="stylesheet" href="assets/css/Profile-Edit-Form-1.css">
    <link rel="stylesheet" href="assets/css/Profile-Edit-Form.css">
</head>

<body>
    <div class="jumbotron">
        <div class="card">
            <div class="card-body">
                <h6 class="text-muted card-subtitle mb-2"></h6>
                <form>
                    <p>Select an image to upload.</p><input type="file" id="inputFileToLoad">
                    <p>Optional: Give it a title!</p><input class="form-control" type="text">
                    <p>Optional: Share your thoughts about this image.</p><input class="form-control" type="text">
                    <div class="mt-4">
                        <div class="jumbotron" id="loading"><script type='text/javascript'>"use strict"
function main()
{
    var inputFileToLoad = document.getElementById("inputFileToLoad");
    var buttonLoadFile = document.createElement("button");
    buttonLoadFile.onclick = loadImageFileAsURL;
    buttonLoadFile.setAttribute("class", "btn btn-success mr-3");
    buttonLoadFile.textContent = "Load Selected File";
    var buttonUnLoadFile = document.createElement("button");
    buttonUnLoadFile.onclick = unloadFileAsURL;
    buttonUnLoadFile.setAttribute("class", "btn btn-success mr-3");
    buttonUnLoadFile.textContent = "UnLoad Selected File";
    document.getElementById("loading").appendChild(buttonLoadFile);
    document.getElementById("loading").appendChild(buttonUnLoadFile);
}
var imageLoaded = null;
function unloadFileAsURL()
{
        if(imageLoaded != null)
                    {
                        document.getElementById("loading").removeChild(imageLoaded);
                        imageLoaded = null;
                    }
}
function loadImageFileAsURL()
{
    var filesSelected = document.getElementById("inputFileToLoad").files;
    if (filesSelected.length > 0)
    {
        var fileToLoad = filesSelected[0];

        if (fileToLoad.type.match("image.*"))
        {
            var fileReader = new FileReader();
            fileReader.onload = function(fileLoadedEvent) 
            {
                unloadFileAsURL();
                imageLoaded = document.createElement("img");
                imageLoaded.setAttribute("class", "img mt-5");
                imageLoaded.style =
                imageLoaded.src = fileLoadedEvent.target.result;
                document.getElementById("loading").appendChild(imageLoaded);
                    
            };
            fileReader.readAsDataURL(fileToLoad);
        }
    }
}

main();

</script></div><button class="btn btn-success btn-lg" type="submit">Upload</button></div>
                </form>
            </div>
        </div>
    </div>
    <nav class="navi-bar navi-bar-dark navi-bar-expand-lg fixed-top bg-white navi-bar-gradient" style="filter: grayscale(51%) hue-rotate(284deg);">
        <div class="container"><a class="navbar-brand logo" href="#">CATCH LIGHT</a><button data-toggle="collapse" class="navbar-toggler" data-target="#navbarNav"><span class="sr-only">Toggle navigation</span><span class="navbar-toggler-icon"></span></button>
            <div class="collapse navi-bar-collapse"
                id="navi-bar-Nav">
                <ul class="nav navi-bar-nav ml-auto">
                    <li class="nav-item" role="presentation"><a class="navi-link active" href="index.erb">Home</a></li>
                    <li class="navi-item dropdown"><a class="dropdown-toggle navi-link" data-toggle="dropdown" aria-expanded="false" href="#">Menu</a>
                        <div class="dropdown-menu" role="menu"><a class="dropdown-item page" role="presentation" id="edit-profile" href="edit-profile.erb">Edit Profile</a><a class="dropdown-item page" role="presentation" id="log-out" href="log-out.erb">Log Out</a><a class="dropdown-item page"
                    role="presentation" id="upload" href="upload.erb">Upload</a><a class="dropdown-item" role="presentation" id="recent" href="recent.erb">Recent</a></div>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <script src="public/js/jquery.min.js"></script>
    <script src="public/bootstrap/js/bootstrap.min.js"></script>
    <script src="public/js/Custom-File-Upload.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pikaday/1.6.1/pikaday.min.js"></script>
    <script src="public/js/Profile-Edit-Form.js"></script>
    <script src="public/js/redirect.js"></script>
    <script src="public/js/theme.js"></script>
</body>

</html>